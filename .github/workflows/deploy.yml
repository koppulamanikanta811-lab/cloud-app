name: Cloud Pipeline

on:
  push:
    branches:
      - main

jobs:

  validate:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Validate Website
        run: |
          echo "Pipeline Running"
          pwd
          ls
          cat index.html
